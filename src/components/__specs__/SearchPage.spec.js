import React from 'react'
import { Text, TouchableOpacity } from 'react-native'
import { shallow } from 'enzyme'

describe('<SearchPage />', () => {

  it.skip('should display current location by default', () => {

  })

  it.skip('should provide a button to search for new starting location (!)', () => {

  })

  it.skip('should provide a list of favourite locations to choose starting location', () => {

  })

  it.skip('should provide a button to search for destinations', () => {

  })

  it.skip('should provide a list of favourite locations to choosing destination', () => {

  })

  it.skip('should display of the current location as the from point', () => {
      // const button = shallow(<Button>Click me</Button>);
      //
      // expect(button.containsMatchingElement(TouchableOpacity)).to.equal(true);
      // expect(button.containsMatchingElement(Text)).to.equal(true);
  })
})
